package cleanCodeTricks;

//https://medium.com/kayvan-kaseb/the-design-rules-of-clean-code-in-software-development-10bae938a80d
public class WithIfElse {
    /*
    double getSpeed() {
        switch (type) {
            case EUROPEAN:
                return getBaseSpeed();
            case AFRICAN:
                return getBaseSpeed() - getLoadFactor() * numberOfCoconuts;
            case NORWEGIAN_BLUE:
                return (isNailed) ? 0 : getBaseSpeed(voltage);
        }
        throw new RuntimeException("Should be unreachable");
    }

    enum Country{
        EUROPEAN,AFRICAN,NORWEGIAN_BLUE
    }
}*/
}

